---
description: 记忆系统元规则——何时以及如何更新知识索引与对话记录
globs: .cursor/memory/**/*.md, docs/**/*.md
---

# 记忆系统更新规则

内部记忆文件统一存放于 `.cursor/memory/`（已加入 `.gitignore`，不会提交到 GitHub）。

## 1. 何时更新

- **功能/代码/配置发生实质性变化**：更新 `.cursor/memory/CURRENT_STATE.md` 的「已完成功能」「最近修改的文件」「下一步计划」。
- **阻塞项或优先级变化**：更新 `.cursor/memory/CURRENT_STATE.md` 的「当前最大阻塞」。
- **完成一次有决策或实现的对话**：在 `.cursor/memory/sessions/` 下新增或追加会话摘要，并更新 `.cursor/memory/sessions/INDEX.md`。
- **新增文档、脚本或重要路径**：在 `.cursor/memory/INDEX.md` 对应层级补充条目。
- **新增或更新计划文档**：同步更新 `.cursor/memory/INDEX.md` 的 plans 部分，并在 `CURRENT_STATE.md` 标记当前活跃计划。

## 2. 更新方式

- **CURRENT_STATE.md**：只改相关小节，保留原有结构；文末「最后更新」改为当前日期。
- **INDEX.md**：在 1/2/3 层下增删改条目，保持 3 层树形与表格格式。
- **plans**：新增计划时必须遵循 `.cursor/memory/plans/000-plan-system-spec.md`；只有测试通过并完成验收记录后，计划状态才能标记为 `done`。
- **sessions**：新会话建 `session-YYYY-MM-DD.md`（若同日多段可加后缀），在 `sessions/INDEX.md` 表里加一行。

## 3. 何时执行更新（触发方式）

- **用户输入「更新索引」**：立即按上述规则更新 `.cursor/memory/CURRENT_STATE.md`、`.cursor/memory/sessions/`、`.cursor/memory/INDEX.md`（若有必要）。无需写 last-index-date，除非希望与「每日首次」一致。
- **每天第一次对话**：`sessionStart` 会在今日尚未更新时在注入内容中加上【今日尚未做记忆更新】提示。请在本轮对话中完成一次记忆更新，并将今日日期（YYYY-MM-DD）写入 `.cursor/hooks/state/last-index-date`（目录不存在则先创建）。
- 其他需要更新记忆时（如完成重要功能/决策后），按第 1 节判断并更新即可。
